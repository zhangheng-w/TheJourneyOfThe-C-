/*
 * Created by weyiua on 2021/6/25.
 * 50 了解new和delete的合理替换时机
 *
 * * 有许多的理由需要写个自定的new和delete，包括改善效能、对heap运用错误进行调试、收集heap的使用信息
 */


/*
 * 用来检测运用上的错误
 * 为了强化效能
 * 为了收集使用上的统计数据
 *
 *
 * 为了检测运用错误
 * 为了收集动态分配内存之使用统计信息
 * 为了增加分配和归还的速度
 * 为了降低缺省内存管理器打起来的空间额外开销
 * 为了弥补缺省分配器中的非最佳齐位
 * 为了将相关对象成簇集中
 * 为了获得非传统的行为
 */


int main(int agrc, const char *agrv[]) {
    return 0;
}